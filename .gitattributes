# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Only include the addon folder in generated source archives.
/**        export-ignore
/addons    !export-ignore
/addons/** !export-ignore
